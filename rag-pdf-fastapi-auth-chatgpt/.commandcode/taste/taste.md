# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# python
- Manage Python dependencies via pip commands (pip install, pip uninstall) instead of directly editing requirements.txt. Confidence: 0.70

# llm
- Use OpenRouter as the LLM API provider (OpenAI-compatible API endpoint with OpenRouter API key) instead of direct OpenAI. Confidence: 0.65

